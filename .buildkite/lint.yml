steps:
  - label: ":lint-roller: Lint Buildkite pipelines (Schema + OPA)"
    key: lint-buildkite-pipelines
    agents:
      queue: OpenTofu
    command: "powershell -NoProfile -ExecutionPolicy Bypass -File .buildkite/scripts/Test-BuildkitePipelines.ps1"
